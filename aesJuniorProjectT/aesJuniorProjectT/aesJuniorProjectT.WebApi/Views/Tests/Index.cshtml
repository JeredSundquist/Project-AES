@model IEnumerable<aesJuniorProjectT.Entities.Shared.Net45.Models.Test>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.question_list)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.passingScore)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.question_list)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.passingScore)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.test_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.test_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.test_id })
        </td>
    </tr>
}

</table>
